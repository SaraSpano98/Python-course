# result = match.group()        # Returns the whole match. Also group(0).
# result = match.group(1)       # Returns part inside the first brackets.
# result = match.groups()       # Returns all bracketed parts.
# start_index = match.start()   # Returns start index of the match.
# end_index = match.end()       # Returns exclusive end index of the match.